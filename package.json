{
  "name": "@afi-ops",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest",
    "lint": "eslint 'src/**/*.ts' 'ops/**/*.ts'"
  },
  "dependencies": {
    "@sentry/node": "^7.108.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.5.0",
    "eslint": "^8.56.0"
  }
}
